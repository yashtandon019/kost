---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: kostconfigs.kost.kost.dev
spec:
  group: kost.kost.dev
  names:
    kind: KostConfig
    listKind: KostConfigList
    plural: kostconfigs
    singular: kostconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KostConfig is the Schema for the kostconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of KostConfig
            properties:
              explainer:
                description: Explainer configures the LLM backend for generating explanations.
                properties:
                  endpoint:
                    description: Endpoint is a custom API endpoint URL (useful for
                      Ollama or proxies).
                    type: string
                  model:
                    description: Model is the specific model to use (e.g., "claude-sonnet-4-20250514",
                      "gpt-4o").
                    type: string
                  provider:
                    default: noop
                    description: 'Provider is the LLM backend to use: "noop", "claude",
                      "openai", or "ollama".'
                    enum:
                    - noop
                    - claude
                    - openai
                    - ollama
                    type: string
                  secretRef:
                    description: |-
                      SecretRef references a Kubernetes Secret containing the API key.
                      The secret must have a key named "api-key".
                    properties:
                      name:
                        description: Name is the name of the Secret.
                        type: string
                    required:
                    - name
                    type: object
                type: object
              minSamples:
                default: 10
                description: |-
                  MinSamples is the minimum number of data points required before
                  anomaly detection activates.
                minimum: 3
                type: integer
              pollingInterval:
                default: 5m
                description: PollingInterval is how often to check for anomalies (e.g.,
                  "5m", "30s").
                type: string
              pricing:
                description: Pricing configures per-resource cost estimation.
                properties:
                  cpuPerCoreHour:
                    default: 0.034
                    description: CPUPerCoreHour is the cost per CPU core per hour.
                    type: number
                  gpuPerHour:
                    default: 0.526
                    description: GPUPerHour is the cost per GPU per hour.
                    type: number
                  memoryPerGBHour:
                    default: 0.0043
                    description: MemoryPerGBHour is the cost per GB of memory per
                      hour.
                    type: number
                type: object
              sigmaThreshold:
                default: 2
                description: |-
                  SigmaThreshold is the number of standard deviations above the mean
                  that triggers an anomaly alert.
                minimum: 0.5
                type: number
            type: object
          status:
            description: status defines the observed state of KostConfig
            properties:
              activeAnomalies:
                description: ActiveAnomalies is the number of currently detected anomalies.
                type: integer
              conditions:
                description: conditions represent the current state of the KostConfig
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastCheckTime:
                description: LastCheckTime is when the last anomaly check was performed.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
